# Teacher Agent

Loop: diagnose -> select_strategy -> generate_worksheet -> evaluate -> update_student

Wire OpenRouter via OPENROUTER_API_KEY / OPENAI_BASE_URL in services/api.
Nodes live under graphs/teacher_agent/nodes/.
